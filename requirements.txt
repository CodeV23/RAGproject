# === Core ===
numpy>=1.26.0
torch>=2.0.0
psutil
rich

# === LangChain ecosystem ===
langchain>=0.2.11
langchain-core>=0.2.5
langchain-community>=0.2.5
langchain-huggingface>=0.0.3
langchain-ollama>=0.1.3
langchain-text-splitters>=0.0.1

# === Embeddings + Vectorstores ===
sentence-transformers>=2.2.2
chromadb>=0.5.3
hnswlib>=0.8.0
datasketch>=1.6.5

# === Wikipedia scraping ===
wikipedia-api>=0.6.0

# === Optional (for GPU FAISS via Conda, not pip) ===
# Install via conda (Windows):
#   conda install -c pytorch faiss-gpu=1.7.4 cudatoolkit=12.1
# For CPU fallback (pip):
#   pip install faiss-cpu
